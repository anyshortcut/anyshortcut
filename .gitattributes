# Keep the archive produced by `npm run package:source` — the one AMO
# reviewers get — to the sources the extension is actually built from.
website export-ignore
website/** export-ignore
.github export-ignore
.github/** export-ignore
.gitattributes export-ignore
