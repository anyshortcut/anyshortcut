export default {
    baseURL: BUILD_SCHEMA + BUILD_DOMAIN,
    apiURL: BUILD_SCHEMA + 'api.' + BUILD_DOMAIN,
    googleAuthURL: BUILD_SCHEMA + BUILD_DOMAIN + '/oauth/google',
    debug: BUILD_DEBUG,
};